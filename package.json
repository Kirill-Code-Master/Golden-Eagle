{
  "name": "golden-eagle",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"API,UI\" --prefix-colors \"yellow.bold,cyan.bold\" \"npm run dev --prefix back\" \"npm run dev --prefix front\"",
    "install:all": "npm install --prefix back && npm install --prefix front && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "overrides": {
    "shell-quote": "^1.8.4"
  }
}