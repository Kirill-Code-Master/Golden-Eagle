export default function Home() {
  return (
    <main className="page">
      <section className="hero">
        <p className="eyebrow">Golden Eagle</p>
        <h1>Frontend is ready</h1>
        <p>
          React app is now placed in the front folder and can be connected to the
          Node.js backend API.
        </p>
      </section>
    </main>
  )
}
